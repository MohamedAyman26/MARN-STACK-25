# 🧱 1. إنشاء مجلد المشروع والدخول إليه
mkdir express-ts-app
cd express-ts-app

# 📦 2. تهيئة npm
npm init -y

# ⚙️ 3. تثبيت Express
npm install express

# 🧩 4. تثبيت TypeScript وأدوات التطوير
npm install --save-dev typescript ts-node @types/node @types/express nodemon

# 🧰 5. إنشاء ملف إعداد TypeScript
npx tsc --init

# ✍️ 6. تعديل ملف tsconfig.json (افتحه وبدله بالآتي)
# -----------------------------------------
# {
#   "compilerOptions": {
#     "target": "es2019",
#     "module": "commonjs",
#     "rootDir": "./src",
#     "outDir": "./dist",
#     "esModuleInterop": true,
#     "strict": true,
#     "skipLibCheck": true,
#     "forceConsistentCasingInFileNames": true,
#     "types": ["node"]
#   },
#   "include": ["src/**/*"],
#   "exclude": ["node_modules"]
# }
# -----------------------------------------

# 📁 7. إنشاء مجلد الكود المصدري
mkdir src

# 🧠 8. إنشاء ملف السيرفر الرئيسي
echo "import express, { Request, Response } from 'express';

const app = express();
const PORT = 5000;

app.get('/', (req: Request, res: Response) => {
  res.send('Hello from Express + TypeScript!');
});

app.listen(PORT, () => {
  console.log(\`🚀 Server running on http://localhost:\${PORT}\`);
});
" > src/index.ts

# 🚀 9. إضافة سكربتات التشغيل داخل package.json
# افتح package.json وعدل القسم \"scripts\" ليكون هكذا:
# -----------------------------------------
# \"scripts\": {
#   \"build\": \"tsc\",
#   \"start\": \"node dist/index.js\",
#   \"dev\": \"nodemon --watch src --exec ts-node src/index.ts\"
# }
# -----------------------------------------

# ▶️ 10. لتشغيل المشروع في وضع التطوير
npm run dev

# 🏗️ 11. أو للبناء (تجميع TypeScript إلى JavaScript)
npm run build

# ▶️ 12. لتشغيل نسخة الإنتاج
npm start
